import Vue from "vue";
import { Button,Input,Form, FormItem,Tabs,TabPane, Table,TableColumn,Select,Option} from "element-ui";

Vue.use(Button).use(Input).use(Form).use(FormItem).use(Tabs).use(TabPane).use(Table).use(TableColumn).use(Select).use(Option);
